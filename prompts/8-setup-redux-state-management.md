# Prompt Log 8: Setup Redux State Management

## Goal

프로젝트 전역 상태 관리를 Redux Toolkit으로 설정한다.

## Scope

- `@reduxjs/toolkit`, `react-redux` 의존성 추가
- `store/`에 기본 store/slice/hooks/provider 구성
- App Router 레이아웃(`DefaultLayout`)에 `ReduxProvider` 연결
- 요구사항 문서/ADR 반영

## Verification

- 타입체크 통과
